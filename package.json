```json
{
  "name": "game-site",
  "version": "0.1.0",
  "description": "A game site using Ultraviolet as a proxy",
  "main": "index.js",
  "scripts": {
    "start": "ultraviolet serve --proxy-url http://localhost:8080",
    "build": "ultraviolet build",
    "watch": "parcel watch src/index.html"
  },
  "dependencies": {
    "particles.js": "^2.0.0",
    "ultraviolet": "^0.2.0"
  },
  "devDependencies": {
    "parcel": "^2.0.0"
  },
  "license": "MIT"
}
```

- Removed the unnecessary `parcels` dependency.